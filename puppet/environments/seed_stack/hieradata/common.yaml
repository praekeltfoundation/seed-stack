classes:
  - common

dcos_node::gluster_nodes: "%{alias('clusterparams.gluster_nodes')}"

xylem_node::gluster_nodes: "%{alias('clusterparams.gluster_nodes')}"

mc2_manager::infr_domain: "%{alias('clusterparams.infr_domain')}"
mc2_manager::hub_domain: "%{alias('clusterparams.hub_domain')}"
